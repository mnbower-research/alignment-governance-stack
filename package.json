{
  "name": "agent-action-governance",
  "version": "0.1.0",
  "private": true,
  "description": "Initial TypeScript monorepo scaffold for PGDL, AAG, runtime binding, receipts, examples, docs, and eval fixtures.",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  },
  "packageManager": "pnpm@9.12.3"
}

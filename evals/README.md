# Evals

Evaluation fixtures live under `fixtures/pgdl` and `fixtures/aag`.

These files are placeholders for future tests that compare proposal maturation, execution gating, and runtime binding behavior against expected decisions.

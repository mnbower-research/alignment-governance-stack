# PGDL to AAG Example

Placeholder example showing the intended flow from proposal maturation to execution gating.

```text
Agent proposal -> PGDL -> resolved proposal or escalation -> AAG -> permit decision
```

Run support is scaffolded in `run.ts`; real policy logic will be added later.
